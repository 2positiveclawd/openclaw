# Template for ~/.openclaw/.secrets.env
# Copy this file to ~/.openclaw/.secrets.env and replace placeholders with real values.

# Azure OpenAI key (Azure Portal → your OpenAI resource → Keys & Endpoint)
AZURE_OPENAI_API_KEY=__AZURE_OPENAI_API_KEY__

# Azure OpenAI endpoint (Azure Portal → your OpenAI resource → Keys & Endpoint)
# Example: https://<resource-name>.openai.azure.com/
AZURE_OPENAI_ENDPOINT=__AZURE_OPENAI_ENDPOINT__

# Discord bot token (Discord Developer Portal → Applications → Bot → Token)
DISCORD_BOT_TOKEN=__DISCORD_BOT_TOKEN__

# Gateway auth token (generate a strong random string; used by OpenClaw gateway)
# Example: openssl rand -hex 32
GATEWAY_TOKEN=__GATEWAY_TOKEN__

# Optional: remote memory search base URL (if using a remote memory-search service)
# Example: https://<host>/v1
REDACTED_ENDPOINT=__REDACTED_ENDPOINT__

# Optional: Notion API key (https://www.notion.so/my-integrations)
REDACTED_API_KEY=__REDACTED_API_KEY__
