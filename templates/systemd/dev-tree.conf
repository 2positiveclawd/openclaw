# Force gateway to use dev tree (has extensions: goal-loop, planner, researcher, trend-scout)
# This override persists even if the main service file is regenerated
[Service]
ExecStart=
ExecStart=/usr/bin/node __REPO_DIR__/openclaw.mjs gateway --bind loopback --port 18789
