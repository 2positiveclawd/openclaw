# OpenClawd Secrets - Mounted into container at /run/secrets/openclaw.env
# Copy to secrets.env and chmod 600
#
# These are loaded at runtime by the gateway

AZURE_OPENAI_API_KEY=your-azure-key
DISCORD_BOT_TOKEN=your-discord-token
