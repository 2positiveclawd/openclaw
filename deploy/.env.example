# OpenClawd Gateway - Environment Configuration
# Copy this file to .env and fill in your values
#
# SECURITY: Never commit .env files with real secrets!

# ============================================================
# REQUIRED - Gateway Security
# ============================================================

# Gateway authentication token (generate with: openssl rand -hex 32)
OPENCLAW_GATEWAY_TOKEN=your-secure-token-here

# ============================================================
# AI Provider API Keys (at least one required)
# ============================================================

# Azure OpenAI (recommended for OpenClawd)
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com

# OpenAI (optional)
OPENAI_API_KEY=

# Anthropic (optional)
ANTHROPIC_API_KEY=

# ============================================================
# Messaging Channels
# ============================================================

# Discord bot token (from Discord Developer Portal)
DISCORD_BOT_TOKEN=

# ============================================================
# Container Settings
# ============================================================

# Gateway port (default: 18789)
GATEWAY_PORT=18789

# Timezone
TZ=UTC

# Secrets file path (mounted read-only into container)
SECRETS_FILE=./secrets.env

# ============================================================
# Host User Mapping (for permission compatibility)
# ============================================================

# Your host username (paths in config are /home/<user>/.openclaw/...)
HOST_USER=azureuser

# Your host UID/GID (run `id` to find these)
HOST_UID=1000
HOST_GID=1000

# ============================================================
# Data Directories
# ============================================================

# Main OpenClaw config directory (contains everything)
# This includes: workspace, workspace-*, goal-loop, planner, agents, browser, etc.
OPENCLAW_CONFIG_DIR=~/.openclaw

# What's in ~/.openclaw that agents use:
# - workspace/           Main agent workspace (files, projects, memory)
# - workspace-travel/    Travel agent workspace
# - workspace-researcher/ Researcher agent workspace
# - workspace-executor/  Executor agent workspace
# - goal-loop/           Goal states and iterations
# - planner/             Planner states and tasks
# - agents/              Agent sessions and logs
# - browser/             Chrome/Puppeteer sessions
# - dashboard/           Trend digests and reports
# - prds/                Product requirement docs
# - researcher/          Research outputs
# - media/               Media files
